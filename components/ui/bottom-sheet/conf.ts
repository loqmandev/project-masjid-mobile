export const DEFAULT_SPRING_CONFIG = {
  damping: 30,
  stiffness: 300,
  mass: 0.5,
};

export const DEFAULT_TIMING_CONFIG = {
  duration: 250,
};

export const HANDLE_HEIGHT = 40;

export const SCROLL_TOP_THRESHOLD = 1;
